/** @jsx React.DOM */

var React = require('react');

var NavBar = React.createClass({
  render: function() {
    return (
      <div className="navBar">
        {"Bellow's Riddles"}
      </div>
      );
  }
});

module.exports = NavBar;